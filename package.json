{
  "name": "lichen",
  "version": "2.0.0",
  "description": "Lightweight Chart Engine",
  "license": "AGPL-3.0-or-later",
  "author": "Jérôme Chèze",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist/* ; tsc --build",
    "clean": "tsc --build --clean",
    "doc": "typedoc --plugin typedoc-plugin-markdown --plugin typedoc-plugin-rename-defaults --entryPointStrategy expand --out docs --name \"lichen\" src"
  },
  "devDependencies": {
    "typedoc": "^0.25.2",
    "typedoc-plugin-markdown": "^3.16.0",
    "typedoc-plugin-rename-defaults": "^0.6.7",
    "typescript": "^4.9.4"
  }
}
